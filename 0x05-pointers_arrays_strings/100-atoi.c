#include "main.h"
#include <stdlib.h>

/**
 * _atoi - coverts string to an integer
 *
 * @s: string to be converted
 *
 * Return: converted integer value
 */

int _atoi(char *s)
{
	return (atol(s));
}
